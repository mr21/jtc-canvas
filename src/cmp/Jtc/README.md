# Jtc

`Jtc` is a React component, instantiable with:
* `React.createElement( Jtc )` or
* `<Jtc/>` in JSX.

This component is a singleton **not intented to be instantiated more than one time at the moment.**

The root HTML element has the unique id: `'jtc'`.

No particular props.
